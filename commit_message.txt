perf: Improve animation performance and mobile navigation smoothness

- Optimize GalaxyBackground canvas: pre-allocate gradients, use time-based animation (rAF), add `will-change` to canvas and nebula blobs for GPU compositing.
- Optimize AtmosphericBlobs: cache parsed values outside rAF loop, add `will-change` and `contain: paint` to CSS classes for early GPU promotion.
- Refine Header transitions: replace `transition-all` with specific properties on header and language/accent buttons to avoid unnecessary recalculations.
- Enhance mobile navigation drawer: use `will-change: transform, opacity`, faster cubic-bezier ease, and shorter duration for crisp animation.
- Implement global `touch-action: manipulation` on `body` and interactive elements to eliminate 300ms tap delay on mobile.